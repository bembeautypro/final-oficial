{
  "scripts": {
    "build:frontend": "vite build --config vite.config.deploy.ts",
    "build:production": "vite build --config vite.config.deploy.ts && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"
  }
}